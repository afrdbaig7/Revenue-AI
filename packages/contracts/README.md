# API Contracts

This package serves as the shared registry for API contract types within the RecoverAI platform.
These TypeScript definitions represent the DTOs (Data Transfer Objects) and data structures exchanged between the frontend (React/Next.js) and the backend services.

The types defined here mirror the backend DTOs to ensure consistency across the stack.
